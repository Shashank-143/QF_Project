To run this project, simply navigate to the project folder and use the following commands:

```bash
uv sync                 # Creates the .venv and installs all dependencies
uv run jupyter notebook  # Launches the notebook in the correct environment

```

Once the notebook is open, ensure you select the kernel located in the **`.venv`** folder to access the installed libraries.